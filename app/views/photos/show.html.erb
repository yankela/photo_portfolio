<p id="notice"><%= notice %></p>

<p>
  <strong>Url:</strong>
  <%= image_tag @photo.url %>
</p>

<p>
  <strong>Gallery:</strong>
  <%= @photo.gallery.title %>
</p>

<%= link_to 'Edit', edit_photo_path(@photo) %> |
<%= link_to 'Back', photos_path %>
